class WebSearch:

    def search(self, query: str):

        """
        Web search-এর architecture placeholder.

        এখানে এখন external search engine/API ব্যবহার করা হচ্ছে না।
        পরবর্তীতে Logy-এর web-search tool এখানে যুক্ত হবে।
        """

        if not query:
            return []

        return []
