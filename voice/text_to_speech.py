class TextToSpeech:

    def speak(self, text):

        """
        Text → Voice system.

        ভবিষ্যতে Logy-এর configured voice এখানে যুক্ত হবে।
        """

        if not text:
            return False

        return True
